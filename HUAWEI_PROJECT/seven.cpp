#include <iostream>
#include <map>
/*
 
 using namespace std;

 int main(int argc, char* argv[])
 {

 int numbers;
 while (cin>>numbers)
 {
 map<int, int> themap;
 //C++是不是都是这样先设置输入的行数啊.
 while (numbers--)
 {
 int key, value;
 cin >> key >> value;
 if (themap[key]!=0)
 {
 themap[key] += value;
 }
 else
 {
 themap[key] = value;
 }
 }
 // auto C++自动判断数据类型.
 for (auto iter = themap.begin();iter!=themap.end();iter++)
 {
 //first,second 用来访问map的成员.
 cout << iter->first << " " << iter->second << endl;
 }
 }


 }

 */
