#include <iostream>
#include <string>
/*
 using namespace std;

 void Processing(string& str)
{
	if (str.size()<8)
	{
		if (!str.empty())
		{
			cout << str.append(8 - str.size(), '0') << endl;
			return;
		}
		return;
	}
	cout << str.substr(0, 8) << endl;
	string left = str.substr(8, str.size() - 8);
	Processing(left);
}

int main(int argc, char* argv[])
{
	string str1, str2;
	cin >> str1 >> str2;
	Processing(str1);
	Processing(str2);
	system("pause");
}
 */



